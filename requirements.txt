# Dependencias del Sistema de Solicitud de Productos (Uso Bodega - Bioplates)

# Procesamiento de datos
pandas>=1.5.0
numpy>=1.23.0
openpyxl>=3.0.10
xlrd>=2.0.1

# Generación de PDFs
reportlab>=3.6.0
Pillow>=9.0.0

# Manipulación de PDFs (para unificación)
pypdf>=3.0.0

# Windows específicas (para impresión)
pywin32>=305; sys_platform == 'win32'
